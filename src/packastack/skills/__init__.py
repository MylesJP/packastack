"""Skill definitions for AI-powered packaging diagnosis.

Each subdirectory contains a ``SKILL.md`` file describing one AI task.
Skills are pure markdown with YAML frontmatter; they contain no code
and work with any OpenAI-compatible chat model.

Add a new skill by creating ``<skill-name>/SKILL.md`` — no Python
changes required for skills that reuse existing context and result
parsing.

See :mod:`packastack.ai.skills` for the loader API.
"""
